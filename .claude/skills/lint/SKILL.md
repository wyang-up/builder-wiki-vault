---
name: lint
description: Use when the user wants to check wiki health, scan the knowledge base for broken links, orphan pages, missing index registrations, or unresolved knowledge conflicts, including explicit /lint, /scan, or /health requests.
user-invocable: true
---

# lint 技能：知识图谱健康巡检

## 核心目标
将软件工程中的“静态代码分析”引入知识管理。定期运行此 skill，找出知识库长期演进中产生的：死链、孤岛、未同步索引、认知冲突。

## 触发逻辑
- 用户输入 `/lint`
- 用户询问“我的知识库健康状况如何”
- 用户要求“检查知识库状态”或“检查健康”

## 知识库路径
- 使用 Glob 工具动态定位当前工作区下的 wiki/ 目录

## 巡检流水线

### 第 1 步：索引一致性检查
1. 读取 `wiki/index.md` 全部内容
2. 扫描 `wiki/` 下所有 `.md` 文件（排除 index.md 和 log.md）
3. 提取 index.md 中注册的所有双链链接 `[[页面名称]]`
4. 比对：找出已注册但文件不存在的条目 OR 文件存在但未注册的页面

### 第 2 步：双向链接健康检查
1. 扫描所有 `.md` 文件，提取所有 `[[双链]]` 格式的链接
2. 读取每个页面 frontmatter 中的 `aliases`；将“文件名（去扩展名）”“完整文件名（含 `.md`）”与别名一起视为合法链接目标集合
3. 额外维护一个极小的“文档保留示例链接”集合；当前包含 `wikilink`、`页面名称`、`PageName`，用于在规则文档中演示 Obsidian 双链语法
4. 如果链接既不匹配任何页面文件名（含去扩展名与完整文件名），也不匹配任何已声明别名，且不在保留示例链接集合中 → 标记为**死链**
5. 统计被引用的页面（排除 self-reference）；若链接命中别名或完整文件名，应归并到其真实页面名下统计；命中保留示例链接集合的占位符不计入引用统计
6. 找出从未被任何其他页面引用的页面 → **孤儿页面**

### 第 3 步：认知冲突审查
1. 全局搜索所有 `.md` 文件中包含 `## 知识冲突` 的页面
2. 提取每个冲突的简要描述（冲突双方是什么）
3. 统计带未解决冲突的页面（认知技术债）

### 第 4 步：收件箱积压检查（可选跳过）
- 本 skill 仅扫描 wiki/，跳过 raw/ 收件箱检查

## 报告输出规范

扫描完成后，输出结构化报告，严格遵循以下格式：

```markdown
## 🩺 知识库健康体检报告 — YYYY-MM-DD

### ✅ 绿灯项
- [运行良好的项目]

### ⚠️ 黄灯项
- **发现 N 个孤儿页面**：[列表] - 建议添加关联或分类
- **发现 N 个未同步索引**：[列表] - 文件存在但未在 index.md 注册

### ❌ 红灯项
- **发现 N 个死链**：[来源页面] → [[不存在的目标页面]]
- **存在 N 个未解决的知识冲突**：[页面名称]

### 🛠️ 下一步行动
1. 是否需要自动修复未同步索引？
2. 是否需要针对知识冲突进行重新推演？
```

## 日志规则

- 即使本次仅执行只读扫描、未进行任何修复，也必须在 `wiki/log.md` 追加一次 `lint` 记录
- 若后续用户确认执行修复，则在修复完成后再追加一次 `sync` 或 `lint` 记录，说明修复内容

## 与 ingest 自动联动的边界

- ingest 后自动联动执行的是本 skill 定义的全量巡检，而不是增量子集
- 自动联动只报告，不自动修复
- 自动联动不单独追加 `lint` 日志
- 用户显式 `/lint` 时，仍执行全量巡检并追加 `lint` 日志

## 硬约束
- **仅读扫描**：生成报告前，禁止修改、删除、重命名任何文件
- **手动确认**：报告后等待用户确认再执行修复
- **静默日志**：日志记录必须遵循 `wiki/log.md` 的既有格式
